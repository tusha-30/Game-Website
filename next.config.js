// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.dummyjson.com'], // Add 'cdn.dummyjson.com' to the allowed domains
    },
  };
  
  module.exports = nextConfig;
  